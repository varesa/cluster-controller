<interface type='bridge'>
    {% match ovn_id %}
        {% when Some with (id) %}
            <source bridge='{{bridge}}' network='ovs' />
            <virtualport type='openvswitch'>
              <parameters interfaceid='{{id}}'/>
            </virtualport>
        {% when None %}
            <source bridge='{{bridge}}'/>
    {% endmatch %}
    {% match queues %}
        {% when Some with (no_queues) %}
            <driver name="vhost" queues="{{no_queues}}"/>
        {% when None %}
    {% endmatch %}
    <mac address="{{mac}}"/>
    <model type="{{model}}"/>
</interface>
